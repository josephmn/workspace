# para cambiarle el nombre al modulo se usa 'as'
#import modulos as xs

# para solo importar solo una parte del modulo (vairables o solo funcion) se usa from
from modulos import mascotas, saludo

print(mascotas)
saludo('Nicolas')